"""Simple module with one top-level function."""


def hello(name: str) -> str:
    """Greet someone by name.

    Args:
        name: Name to greet.

    Returns:
        Greeting string.
    """
    return f"Hello, {name}!"
